export default {
  plugins: {
    // Left empty. A local file in the project root stops postcss-load-config from searching
    // parent directories (e.g., C:\Users\<Name>\Downloads) for stray, unconfigured plugins.
    // Tailwind CSS v4 compiles directly using `@tailwindcss/vite` in the bundler core.
  },
};
